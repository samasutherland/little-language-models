# Training hyperparameters

batch_size = 24
loss = "CrossEntropyLoss"
optimizer = "Adam"
test_prompt = "The dog"
base_lr = 1e-4
total_steps = 5000

scheduler = "Cosine"
warmup_steps = 50

[scheduler_kwargs]
eta_min = 1e-5