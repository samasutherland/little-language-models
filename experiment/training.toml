# Training hyperparameters

batch_size = 32
accumulated_batch_size = 512
loss = "CrossEntropyLoss"
optimizer = "AdamW"
test_prompt = "The dog"
base_lr = 1e-4
total_steps = 5000

scheduler = "OneCycleLR"
warmup_steps = 50

[scheduler_kwargs]
eta_min = 1e-5